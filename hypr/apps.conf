$uwsm = uwsm app --
 
$terminal = $uwsm kitty
$fileManager = $uwsm thunar
# $menu = $uwsm wofi --show drun
$menu = walker
$bar_menu = $uwsm nwg-bar
$browser = $uwsm thorium-browser-avx2
$screenshot = $uwsm grimblast --freeze save area - | swappy -f -
$emoji_picker = $uwsm wofi-emoji
$color_picker = $uwsm hyprpicker
